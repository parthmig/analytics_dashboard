This is a dashboard built using React.js and Victory. I used Python for analysing large csv files and processing the data to find meaningful insights to display on the dashboard.

To run the applcation:

npm start
